# Telegraf pipeline for ECT telemetry ingest.

[agent]
  interval = "1s"
  flush_interval = "1s"
  metric_batch_size = 1000
  metric_buffer_limit = 20000
  omit_hostname = true

[[inputs.mqtt_consumer]]
  servers = ["tcp://127.0.0.1:1883"]
  topics = ["telemetry/eco_archers/events"]
  qos = 1
  client_id = "ect-telegraf-timescale"
  persistent_session = true
  connection_timeout = "30s"
  data_format = "json_v2"

  [[inputs.mqtt_consumer.json_v2]]
    measurement_name = "telemetry_raw"

    [[inputs.mqtt_consumer.json_v2.object]]
      path = "@this"
      tags = ["session_uid", "lap_number"]
      # Everything else automatically drops into the metric field engine
      included_keys = [] 

[[inputs.mqtt_consumer]]
  servers = ["tcp://127.0.0.1:1883"]
  topics = ["telemetry/eco_archers/sessions"]
  qos = 1
  client_id = "ect-telegraf-sessions"
  persistent_session = true
  connection_timeout = "30s"
  data_format = "json_v2"

  [[inputs.mqtt_consumer.json_v2]]
    measurement_name = "sessions_ingest_view"

    [[inputs.mqtt_consumer.json_v2.object]]
      path = "@this"
      disable_prepend_keys = true
      included_keys = [
        "uid",
        "session_name",
        "vehicle_setup"
      ]

# Convert dynamic fields into narrow signal_name/value columns
[[processors.unpivot]]
  namepass = ["telemetry_raw"]
  tag_key = "signal_name"
  value_key = "value"

[[outputs.postgresql]]
  # Replace before deployment.
  connection = "host=127.0.0.1 port=5432 user=postgres password=postgres dbname=telemetry sslmode=disable"
  timestamp_column_name = "time"
  tagexclude = ["topic"]
